# Create directory for colorschemes
mkdir -p ~/.vim/colors && cd ~/.vim/colors

# Get colorscheme
wget https://raw.githubusercontent.com/NLKNguyen/papercolor-theme/master/colors/PaperColor.vim

# NOTE: If you don't want to use it, comment out the `colorscheme` setting in vimrc
